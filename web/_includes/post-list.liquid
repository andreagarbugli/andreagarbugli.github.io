<div id="post-list">
  {% for item in list %}
    <a href="{{ item.url }}" class="post-card">
      <div class="post-meta">{{ item.date | readableDate }}</div>
      <h3>{{ item.data.title }}</h3>
      <p>{{ item.data.summary }}</p>
    </a>
  {% endfor %}
</div>
